import 'package:flutter/material.dart';

class AppConstants {
  //Light mode colors
  static const backgroundColor = Colors.white;
  static Color mainTextColor = Colors.teal.shade900;
  static Color subTextColor = Colors.teal.shade400;
  static Color iconColor = Colors.black;
  static Color containerColor = Colors.teal.shade100.withAlpha(100);
  //Dark mode colors
  static const backgroundColorDark = Color.fromARGB(255, 18, 24, 24);
  static Color mainTextColorDark = Color.fromARGB(255, 234, 252, 249);
  static Color iconColorDark = Colors.white;
  static Color subTextColorDark = Color.fromARGB(255, 182, 205, 203);
  static Color containerColorDark =
      Color.fromARGB(255, 129, 142, 141).withAlpha(100);

  //Text Styles
}
